Autofs configuration for odroid 

Instruction from https://help.ubuntu.com/community/Autofs

# Installation 

$ sudo apt-get install nfs-common
$ sudo apt-get install autofs

Then edit /etc/master accordingly to the example in this repo (remember to comment the two +dir) and 
add /etc/auto.nfs with the content of this repo 

